# AXA 정비수가 자동체결 시스템

Railway 배포용 React 애플리케이션

## 🚀 Railway 배포 방법

### 1. GitHub에 코드 푸시
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin <your-github-repo-url>
git push -u origin main
```

### 2. Railway 프로젝트 생성
1. [Railway](https://railway.app)에 로그인
2. "New Project" 클릭
3. "Deploy from GitHub repo" 선택
4. 해당 저장소 선택

### 3. 환경 설정 (자동)
Railway가 자동으로 다음을 감지합니다:
- Node.js 프로젝트
- 빌드 명령: `npm install && npm run build`
- 시작 명령: `npm start`

### 4. 배포 완료
- Railway가 자동으로 빌드하고 배포합니다
- 몇 분 후 제공되는 URL로 접속 가능합니다

## 📦 프로젝트 구조

```
railway-app/
├── src/
│   ├── App.jsx          # 메인 React 컴포넌트
│   ├── main.jsx         # React 진입점
│   └── index.css        # 스타일시트
├── index.html           # HTML 템플릿
├── server.js            # Express 프로덕션 서버
├── vite.config.js       # Vite 빌드 설정
├── package.json         # 의존성 및 스크립트
├── railway.json         # Railway 설정
└── .gitignore
```

## 🛠️ 로컬 개발

```bash
# 의존성 설치
npm install

# 개발 서버 시작 (포트 3000)
npm run dev

# 프로덕션 빌드
npm run build

# 프로덕션 서버 시작
npm start
```

## 🔧 주요 기능

- ✅ 사용자 인증 (로그인/회원가입)
- ✅ 협력업체 조회 및 계약 생성
- ✅ 계약서 생성 및 관리
- ✅ 협력업체 서명 시뮬레이터
- ✅ 계약 체결 완료 처리

## 🔐 기본 로그인 정보

- 이메일: `admin@axa.co.kr`
- 비밀번호: `admin`

## 🌐 Railway 배포 상태 확인

배포 후 다음을 확인하세요:
1. **Deployment Logs**: 빌드 과정 확인
2. **Runtime Logs**: 서버 실행 상태 확인
3. **Settings > Networking**: 포트가 올바르게 설정되었는지 확인

## ⚠️ 문제 해결

### "Application failed to respond" 오류
- Railway 로그에서 `🚀 Server is running` 메시지 확인
- PORT 환경변수가 자동으로 설정되는지 확인
- 빌드가 성공적으로 완료되었는지 확인

### 빌드 실패
```bash
# 로컬에서 먼저 테스트
npm install
npm run build
npm start
```

## 📝 참고사항

- Node.js 18 이상 필요
- Railway는 자동으로 PORT 환경변수를 설정합니다
- 정적 파일은 `dist/` 디렉토리에서 제공됩니다
